<?php

    // array_change_key_case($array, $case)
    //Chuyển tất cả các key trong mảng $array sang chữ hoa nếu $case = 1 và sang chữ thường nếu $case = 0. 
    //  Ta có thể dùng hằng số CASE_UPPER thay cho số 1 và CASE_LOWER thay cho số 0.

    //6. array_pad($array, $size, $value)
    //Kéo dãn mảng $array với kích thước là $size, 
    //và nếu kích thước truyền vào lớn hơn kích thước mảng $array thì giá trị $value được thêm vào, 
    //gược lại nếu kích thước truyền vào nhỏ hơn kích thước mảng $array thì sẽ giữ nguyên. 
    //Nếu muốn giãn ở cuối mảng thì $size có giá trị dương, nếu muốn giãn ở đầu mảng thì $size có giá trị âm.

?>